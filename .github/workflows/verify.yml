name: Verify

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  check:
    name: Deterministic (Ubuntu / Node ${{ matrix.node }})
    strategy:
      fail-fast: false
      matrix:
        node: [22, 24, 26]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ matrix.node }}
      - run: npm ci
      - run: npm run check

  platform-smoke:
    name: Platform smoke (${{ matrix.os }} / Node 22)
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
      - run: npm ci
      - run: npm run build:plugin
      - name: Check generated plugin files
        run: >-
          git diff --exit-code --
          plugins/hope-commit/LICENSE
          plugins/hope-commit/NOTICE
          tools/plugin-package-files.txt
      - name: Check staged plugin paths
        run: node --test test/platform-smoke.test.mjs

  browser:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm run check:readme-assets
      - run: npm run test:browser

  required:
    name: Verify
    if: ${{ always() }}
    needs: [check, platform-smoke, browser]
    runs-on: ubuntu-latest
    steps:
      - name: Check every supported system and browser
        env:
          BROWSER_RESULT: ${{ needs.browser.result }}
          CHECK_RESULT: ${{ needs.check.result }}
          PLATFORM_SMOKE_RESULT: ${{ needs.platform-smoke.result }}
        run: |
          test "${CHECK_RESULT}" = "success"
          test "${PLATFORM_SMOKE_RESULT}" = "success"
          test "${BROWSER_RESULT}" = "success"
