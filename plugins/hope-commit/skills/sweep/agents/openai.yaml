interface:
  display_name: "Hope Sweep"
  short_description: "Plan decision-ready codebase maintenance"
  default_prompt: "Use $hope-commit:sweep to inspect this codebase and return a decision-ready, evidence-linked maintenance plan without editing files."
